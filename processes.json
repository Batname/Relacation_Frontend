{
  "apps" : [{
    "name"        : "app",
    "script"      : "index.js",
    "node_args"   : "--harmony",
    "cron_restart"     : "* 1 * * *",
    "watch"            : false,
    "max_memory_restart" : "350M",
    "env": {
        "NODE_ENV": "production",
        "PORT": "8080"
    }
  }]
}